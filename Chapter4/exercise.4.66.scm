; My guess is that the frames produced by query might have 
; results bot with unbound variables or non unique results.
; To fix it, we might wait the final result of the query and
; then run accumalation functions. But we would need
; to unify the results by filtering distinct results only.


