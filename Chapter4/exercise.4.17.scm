;(partiall answer) changes in frame structure won't change the
;logic of a program because frames are hierarchical and
;search occurs bottom-up fashion. So that
;creating new frames that has references to uppler levels,
;won't change the program logic.
