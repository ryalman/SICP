; I supports Ben's viewpoint. Because sequential evaluation is
; simpler than others.

; Hoisting simple definitions will result the behaivour 
; anticipated by Eva. Moving (define a 5) above (define b (+ a x))
; will solve the problem.
