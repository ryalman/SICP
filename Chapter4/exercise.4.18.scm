;It will signal an error since the values are not assigned.
;lambda must be called with arguments and it should ignore the arguments
;and work with let-->lambda transformations without using that arguments
;
