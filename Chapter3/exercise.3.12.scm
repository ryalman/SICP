; The first response will be ('b) because append will not affect the value of x
; Instead it will create a new list merging x and y
;
; The second one will change the value of x so that (cdr x) will print
; ('b 'c 'd)
